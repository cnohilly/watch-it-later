{{!-- {{#each popMovies as |content-info|}}
{{> content-card content-info}}
{{/each}} --}}
<section class="mt-6" id="pop-movies-section">
    <h3 class="title has-text-centered is-size-4 pop-movies">Popular Movies</h3>
    <div class="content-row pb-6 pl-4">
        <div class="container">
            <div class="columns mt-5 is-8 is-variable card-container" id="pop-movies-container">
                {{#each popMovies as |content-info|}}
                {{> content-card content-info}}
                {{/each}}
            </div>
        </div>
    </div>
</section>
<section class="mt-6" id="top-movies-section">
    <h3 class="title has-text-centered is-size-4 top-movies">Top Rated Movies</h3>
    <div class="content-row pb-6 pl-4">
        <div class="container">
            <div class="columns mt-5 is-8 is-variable card-container" id="top-movies-container">
                {{#each topMovies as |content-info|}}
                {{> content-card content-info}}
                {{/each}}
            </div>
        </div>
    </div>
</section>
<section class="mt-6" id="pop-tv-section">
    <h3 class="title has-text-centered is-size-4 pop-tv">Popular TV</h3>
    <div class="content-row pb-6 pl-4">
        <div class="container">
            <div class="columns mt-5 is-8 is-variable card-container" id="pop-tv-container">
                {{#each popTV as |content-info|}}
                {{> content-card content-info}}
                {{/each}}
            </div>
        </div>
    </div>
</section>
<section class="mt-6" id="top-tv-section">
    <h3 class="title has-text-centered is-size-4 top-tv">Top Rated TV</h3>
    <div class="content-row pb-6 pl-4">
        <div class="container">
            <div class="columns mt-5 is-8 is-variable card-container" id="top-tv-container">
                {{#each topTV as |content-info|}}
                {{> content-card content-info}}
                {{/each}}
            </div>
        </div>
    </div>
</section>